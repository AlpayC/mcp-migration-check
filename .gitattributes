# Normalize to LF in the repository. The skill's bundled engine is generated by
# esbuild and compared byte-for-byte in CI, so a CRLF checkout on Windows would
# make that check fail for reasons that have nothing to do with the code.
* text=auto eol=lf
